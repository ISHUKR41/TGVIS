# ==========================================================================
# TGVIS — Robots.txt
# ==========================================================================
# This file tells search engine crawlers (Google, Bing, etc.) which pages
# they are allowed to access and crawl. It also points them to the sitemap.
#
# RULES:
# - Allow all pages to be crawled (we want maximum visibility)
# - Block login pages from search results (private)
# - Point to our XML sitemap for faster discovery
# ==========================================================================

# Allow all search engines to crawl the entire site
User-agent: *
Allow: /

# Block every login portal from search results. These pages are private
# workflows, are marked noindex in their HTML, and are not useful landing
# pages for families searching for the school.
Disallow: /pages/login/

# Block utility files from being indexed
Disallow: /_copy_images.py
Disallow: /detail.txt
Disallow: /screenshots/
Disallow: /.cache/
Disallow: /.local/
Disallow: /.agents/

# Point Google to our sitemap for faster page discovery
Sitemap: https://tgvis-bihta.vercel.app/sitemap.xml

# Additional crawler-specific rules
User-agent: Googlebot
Allow: /

User-agent: Bingbot
Allow: /