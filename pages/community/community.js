/* ==========================================================================
   TGVIS — Community Page JavaScript
   Simple page — no complex interactions needed.
   GSAP card animations are handled by global animations.js.
   ========================================================================== */

document.addEventListener('DOMContentLoaded', () => {
  /* No page-specific JS needed — all animations handled by global scripts */
});
